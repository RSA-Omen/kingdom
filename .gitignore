# Local environment
.env
.env.local
.env.*.local
.venv
venv/
__pycache__/
*.pyc
node_modules/
dist/
build/

# Secrets and credentials
.bash_history
.zsh_history
.ssh/
.aws/
.config/gh/
.config/git/credentials

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Submodule tracking (handle separately)
Management/
Operations/
Platform/
admin-center/
.next/

# Per-machine Claude Code state (worktrees, settings.local.json, etc.)
.claude/

# Runtime cache written by the Hand of the King for the dashboard
.hand-snapshot.json

# Lord Chamberlain runtime cache — Asana custom-field enum GIDs, refreshed daily
council/lord-chamberlain/.cf_cache.json

# Capital API runtime data — live SQLite DB, WAL files, backups
capital/api/data/
