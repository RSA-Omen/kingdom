# Flow

_Stub — to be replaced with a real flow diagram._

When an implied-link Pronto URL arrives, the Interceptor matches the pattern, looks the target up in NextCloud, and redirects the user there. Anything that doesn't match the pattern passes through to Pronto's normal handler untouched.

A demo of the current flow will live in `demos/` once drawn.
